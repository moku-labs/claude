# @moku-labs/worker — Plugin & Property Index

> **STUB — regenerated by `moku-sync worker`.** Placeholder until sync reads `../worker`
> (npm `@moku-labs/worker`, latest **0.4.0** at registration) and writes the real catalog here. The
> format mirrors [`skills/moku-web/references/plugin-index.md`](../../moku-web/references/plugin-index.md)
> — framework API form, exports, the plugin table (name / config / state / API / events), and per-plugin
> property index.

## Pending generation

Run **`moku-sync worker`**. Until then the authoritative surface is the package source (`../worker` →
`src/`, `package.json` `exports`, release notes at `frameworks[worker].releaseSource`).

**Known at registration (2026-06-20):** Layer-2 framework depending on `@moku-labs/core@0.1.4` (+
`@moku-labs/common@0.2.0`); Cloudflare plugins for **Durable Objects, Queues, R2, D1, KV**; engines
node ≥24 / bun ≥1.3.14. `knownVersion` is the `0.0.0` "never-synced" sentinel, so the first
`moku-sync worker` run regenerates this file in full and stamps the real version.
