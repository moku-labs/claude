# @moku-labs/room — Plugin & Property Index

> **STUB — regenerated by `moku-sync room`.** Placeholder until sync reads `../room`
> (npm `@moku-labs/room`, latest **0.1.1** at registration) and writes the real catalog here. The format
> mirrors [`skills/moku-web/references/plugin-index.md`](../../moku-web/references/plugin-index.md) —
> framework API form, exports, the plugin table (name / config / state / API / events), and per-plugin
> property index.

## Pending generation

Run **`moku-sync room`**. Until then the authoritative surface is the package source (`../room` →
`src/`, `package.json` `exports`, release notes at `frameworks[room].releaseSource`).

**Known at registration (2026-06-20):** Layer-2 framework **built on `@moku-labs/web`** (peer dep
`^1.12.4`; upgrade order core → web → room) plus `trystero` (WebRTC) + `qrcode`; engines node ≥24 /
bun ≥1.3.14. `knownVersion` is the `0.0.0` "never-synced" sentinel, so the first `moku-sync room` run
regenerates this file in full and stamps the real version.
