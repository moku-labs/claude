---
type: llm
---

PASS if the response says that when Astra (Codex) is unavailable the orchestrating session reviews the same desktop and mobile screenshots itself, using the same findings format or schema, that the report names which reviewer produced the findings, and that the UX gate still runs rather than being skipped.
FAIL if the response says the UX review is skipped, postponed, or degraded to nothing when Astra is missing, or if it never mentions that the same findings format is used by the fallback reviewer.
