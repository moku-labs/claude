---
max_turns: 12
allowed_tools: [Read, Glob, Grep, Skill]
tags: [core, smoke]
---

I'm building on a moku framework. Show me the complete code for a tiny plugin that counts button clicks: it keeps a number in state and exposes `increment()` and `get()`. One file is fine.
