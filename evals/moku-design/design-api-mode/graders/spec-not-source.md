---
type: llm
---

PASS if the response treats the outcome as a design specification — an API contract to be captured in design-context.md and re-implemented from scratch by the builder — and does not present the snippets as the plugin's source.
FAIL if it writes the plugin implementation, creates source files, or tells the user the snippets are the code to keep.
