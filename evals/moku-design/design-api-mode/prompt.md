---
# A pack depends on the core plugin, so both are loaded for the run.
plugins: ["../../../plugins/moku-design", "../../../plugins/moku"]
max_turns: 14
allowed_tools: [Read, Glob, Grep, Skill, Bash]
tags: [design, api]
---

I'm about to add a `streaks` plugin to my moku framework and I want to get its API right before any code exists. Don't start the work yet. In one reply, tell me how you would run that design: what you would produce, in what order, how I would choose, and what I end up with.
