---
type: llm
---

PASS if the response adds a separate createApp imported from "@moku-labs/native" (for example src/native.ts) beside the existing web app, passes the composed system plugins by name as config.system ("store" and "deep-link", ideally from one shared file both apps import), sets capabilities["deep-link"] to { mode: "scheme", scheme: ... } because deep-link needs a scheme, names "ios" in targets because the default is only the host desktop target, and runs the build through the typed verb surface: start(), then native.cli.build({ target: "macos" }) and native.cli.build({ target: "ios", simulator: true }), then stop(), wired as package.json scripts. It should treat the generated Tauri project (.moku/tauri/) and dist-native/ as gitignored build output. It may mention that a real node binary must be on PATH or that native.cli.doctor() checks the toolchain.
FAIL if the response puts native plugins such as tauriPlugin or cliPlugin into the web app's plugins array, imports createApp for the native app from @moku-labs/web or @moku-labs/core, hand-writes or commits src-tauri files such as tauri.conf.json, Cargo.toml or the capabilities JSON, runs "tauri build", "bunx tauri" or "tauri init" directly, invents a command-line tool with flags such as "moku-native build --target ios", uses API names that do not exist (tauri.version(), project.completeness(), web.dev.command), puts passwords or keystore secrets into the signing config, or claims a simulator build needs signing credentials.
